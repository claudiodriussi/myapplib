name: myapplib
description: A collection of classes and function useful to develop Flutter apps
version: 0.0.1
homepage: none

environment:
  sdk: ">=2.18.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  path_provider: ^2.0.15
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  intl: ^0.18.0
  path: ^1.8.3
  reactive_forms: ^16.1.1
  reactive_date_time_picker: ^3.0.0
  i18n_extension: ^10.0.1
  i18n_extension_importer: ^0.0.6
  sqflite:
  sqflite_common_ffi:
  # http: ^0.13.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
